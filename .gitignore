# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib60/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
# Usually these files are written by a python script from a template
# before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
pytestdebug.log

# Translations
*.mo
*.pot

# Django stuff:
*.log
db.sqlite3
db.sqlite3-journal
local_settings.py
media/ # If you store user-uploaded media locally and don't want to track it
staticfiles/ # Generated by collectstatic

# Environment variables
.env
.env.*
!.env.example # Often good to have an example .env file tracked

# Virtual Environment
.venv/
venv/
ENV/
env/
env.bak/
venv.bak/

# IDE / Editor specific files
.idea/ # JetBrains IDEs (PyCharm, IntelliJ)
.vscode/ # Visual Studio Code
*.sublime-project
*.sublime-workspace
nbproject/ # NetBeans
*.swp # Vim swap files
*~ # Backup files
*.bak

# OS generated files
.DS_Store # macOS
Thumbs.db # Windows
ehthumbs.db # Windows
Desktop.ini # Windows

# Celery
celerybeat-schedule
*.pid # Celery worker/beat PIDs

# Jupyter Notebook
.ipynb_checkpoints

# Secrets - although .env should cover most, be explicit if needed
# credentials.json
# secrets.yaml

# Sass / SCSS cache
.sass-cache/
.node-sass-cache/

# Node.js (if you have any frontend build process in the same repo)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json # Sometimes tracked, sometimes not, depending on team policy
yarn.lock # Sometimes tracked, sometimes not

# If using Docker
docker-compose.override.yml # Local overrides
.dockerignore # Docker has its own ignore file, but good to have here too for context

# Terraform (if managing infrastructure)
.terraform/
*.tfstate
*.tfstate.backup

# MyPy cache
.mypy_cache/