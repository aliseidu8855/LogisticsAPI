
# Core Django and DRF
django
djangorestframework
whitenoise
django-cors-headers

# Database Connector (PostgreSQL)
psycopg2-binary
django-filter

# JWT Authentication
djangorestframework-simplejwt

# Asynchronous Task Queue (for notifications, background jobs)
celery
redis # Common broker/backend for Celery, also useful for caching

# Django Caching with Redis (if you choose Redis for caching)
django-redis

# Environment Variable Management
python-dotenv

# API Documentation (recommended)
drf-yasg

# WSGI HTTP Server (for deployment)
gunicorn
# Database configuration
dj_database_url
# Optional: For Celery monitoring (if you use Celery)
# flower